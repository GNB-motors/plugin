# All paths owned by @ItsDevayan. Single-owner setup so only one person
# receives review-request notifications and is the merge gatekeeper.
# Add more owners per-path if/when other contributors take ownership of a
# specific area — don't restore the global team list.

*                                          @ItsDevayan
